# OpenClaw Runtime Helpers

This directory holds runtime-oriented helper files.

The current project keeps the main sync entry point in:

- [`scripts/setup_openclaw_runtime.ps1`](/D:/yuchangyuan/Documents/openclaw-proj/scripts/setup_openclaw_runtime.ps1)

This runtime layer exists so the repository has an explicit OpenClaw-facing structure,
separate from the shared Python service layer.
